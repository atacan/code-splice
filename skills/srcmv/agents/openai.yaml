interface:
  display_name: "CodeSplice"
  short_description: "Semantically select, move, and copy exact bytes"
  default_prompt: "Use $codesplice to select and move an existing code declaration safely between workspace files."
